export let array1 = (arr) => {
  let a1 = arr.join(",");
  return `"${a1}"`;
  //console.log(...arr);
};
// prop drilling,redux,usecontext,rtk
// component -> render
// if it's own state variable changes
// If it's one of parent renders
