export function isGreaterThan18(x,y){
    if(x<y){
        let x1=`${x} is not greater than ${y}`
        return x1
    }
    else{
        let x2=`${x} is greater than ${y}`
        return x2
    }
}